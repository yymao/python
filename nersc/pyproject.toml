[project]
name = "nersc"
version = "2026.05.12"
requires-python = ">=3.14"
dependencies = [
    "core",
    "torch>=2.11",
    "torchvision>=0.26",
    "torchmetrics>=1.9",
    "lsstdesc-gcr-catalogs[full]",
]

[tool.uv.sources]
core = { path = "../core", package = false }

